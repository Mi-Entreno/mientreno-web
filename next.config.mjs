/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ["fell-spray-extras-cure.trycloudflare.com"],
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
